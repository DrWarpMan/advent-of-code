const cmd = "node <year>/<day>";

console.log(`
********************************************************************

Use "${cmd}" to run specific script

********************************************************************`)